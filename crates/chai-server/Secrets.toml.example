# Shuttle.rs secrets configuration
# Copy this file to Secrets.toml and fill in your values
# DO NOT commit Secrets.toml to version control!

# JWT secret for token signing (generate a strong random string)
JWT_SECRET = "your-jwt-secret-here"

# WebAuthn Relying Party ID (your domain without protocol)
RP_ID = "chai-server.shuttleapp.rs"

# WebAuthn origin (full URL with protocol)
RP_ORIGIN = "https://chai-im.vercel.app"
